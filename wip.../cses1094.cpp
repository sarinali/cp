#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int M = 2*1e5;
int n;
ll ar[M+1];
int main() {
    cin.tie(0); cin.sync_with_stdio(0);
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> ar[i];
    }
    int swaps;
    // bubble sort it?
    return 0;
}
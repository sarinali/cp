#include <bits/stdc++.h>
using namespace std;
string s;
int main() {
    cin.tie(0); cin.sync_with_stdio(0);
    for (int i = 0; i < 5; i++) {
        getline(cin, s);
        for (int j = 0; j < s.length(); j++) {
            if (s[j] == 47 && s[j+1] == 47) {
                
            }
        }
    }
    return 0;
}
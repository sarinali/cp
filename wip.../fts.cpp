#include <bits/stdc++.h>
using namespace std;
const int M = 1e6;
int n, ar[M], ans;
string t;
int main() {
    cin.tie(0); cin.sync_with_stdio(0);
    cin >> n;
    cin >> t;
    for (int i = 0; i < n; i++) {
        ar[i] = t[i] - '0';
    }
    for (int i = 0; i < n; i++) {
        // um come back maybe
    }
    return 0;
}